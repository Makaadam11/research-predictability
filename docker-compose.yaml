version: "3.8"
services:
  backend:
    build: ./backend
    env_file: ./backend/.env
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data:/data

  frontend:
    build:
      context: ./frontend
    restart: unless-stopped
    ports:
      - "80:3000"